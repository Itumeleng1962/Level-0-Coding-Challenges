function getMax(num1, num2, num3) {
  if (num1 > num2 && num2 > num2) {
    maxValue = num1;
  } else if (num2 > num2) {
    maxValue = num2;
  } else {
    maxValue = num3;
    return maxValue;
  }
}
console.log(getMax());
